# Generate with: openssl rand -hex 32
NEXTAUTH_SECRET=replace-me
NEXTAUTH_URL=https://xquant.us

# Directory containing CSV files pushed from mtus
PERF_DATA_DIR=/var/lib/perf-site/data

# Optional: where the SQLite users.db lives (defaults to cwd)
PERF_USERS_DB=/var/lib/perf-site/users.db
